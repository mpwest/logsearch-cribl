{
  "name": "logsearch",
  "version": "1.0.0",
  "description": "Log search API implemented for Cribl",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --env-file=.env --import tsx --test src/test/*test.ts",
    "start": "node --env-file=.env dist/index.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "repository": {
    "type": "git",
    "url": "logsearch-cribl"
  },
  "author": "Mark West",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
